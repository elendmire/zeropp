"""BLOCKED: needs EUPPBench raw files on disk (see 01_download_data.sh) before
this script can be implemented. Do not fill this in with placeholder logic —
implement for real once 01_download_data.sh output exists."""

import sys


def main() -> None:
    raise NotImplementedError(
        "blocked: needs downloaded EUPPBench raw files from 01_download_data.sh"
    )


if __name__ == "__main__":
    sys.exit(main())
